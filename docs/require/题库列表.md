# 题库列表

> Repo: https://github.com/hbnjwin/solo-coder
> 项目路径: projects/trae-solo/

---

## q27 - 审批流程可视化编排器

| 字段     | 值                           |
| -------- | ---------------------------- |
| 名称     | 审批流程可视化编排器         |
| 分支名字 | workflow-visual-editor       |
| 任务类型 | 0-1 代码生成                 |
| 业务领域 | Web 前端                     |
| 修改范围 | 跨模块多文件                 |
| 难度     | 困难                         |
| 技术栈   | Vue 3, Vite, TypeScript, SVG |
| 添加时间 | 2026-05-12 09:00             |
| 已交付   | 否                           |

**题目：**
基于 Vue3 + TypeScript 实现审批流程可视化编排器，支持拖拽节点、条件分支双出口连线、属性面板联动、JSON 序列化与反序列化

**Prompt：**
帮我做一个审批流程可视化编排器，用 Vue3 + TypeScript 实现。核心功能是这样的：左侧是节点面板，有"审批节点""条件分支""会签节点""抄送节点"四种类型，可以拖拽到中间的画布区域。画布上节点之间用连线表示流转关系，条件分支节点有两条出口线，需要标注条件表达式。右侧是属性面板，选中节点后可以编辑审批人、超时时间、条件规则等属性。画布要支持缩放和平移，节点可以删除和移动，连线可以删除。拖拽时要有吸附对齐效果。整个编排器的数据结构要能序列化为 JSON，提供一个导出按钮可以复制 JSON 到剪贴板，也要能从 JSON 导入恢复画布。不需要后端，纯前端实现，数据存在 localStorage。

---

## q28 - 审批数据实时监控大屏

| 字段     | 值                               |
| -------- | -------------------------------- |
| 名称     | 审批数据实时监控大屏             |
| 分支名字 | approval-dashboard               |
| 任务类型 | 0-1 代码生成                     |
| 业务领域 | 数据分析与可视化                 |
| 修改范围 | 跨模块多文件                     |
| 难度     | 较难                             |
| 技术栈   | Vue 3, Vite, TypeScript, ECharts |
| 添加时间 | 2026-05-12 09:00                 |
| 已交付   | 否                               |

**题目：**
基于 Vue3 + ECharts 实现审批数据实时监控大屏，包含5种图表、模拟数据刷新、数字滚动动画、暗色主题自适应

**Prompt：**
帮我做一个审批数据实时监控大屏，用 Vue3 + ECharts 实现。页面顶部显示四个统计卡片：今日审批总量、平均审批时长、当前待审批数、超时预警数，数字要有滚动动画效果。中间区域左边是审批趋势折线图（按小时展示24小时数据），右边是各部门审批通过率柱状图。下方左边是审批类型饼图，右边是审批耗时分布热力图（横轴是小时，纵轴是部门，颜色深浅表示平均耗时）。所有图表每5秒模拟刷新一次数据，数据用 mock 随机生成但要有合理趋势。页面要自适应窗口大小，暗色主题，标题栏显示当前时间并每秒更新。顶部统计卡片变化时要有数字跳动动画。

---

## q29 - 审批路由条件引擎升级

| 字段     | 值                       |
| -------- | ------------------------ |
| 名称     | 审批路由条件引擎升级     |
| 分支名字 | routing-condition-engine |
| 任务类型 | Feature 迭代             |
| 业务领域 | 命令行工具               |
| 修改范围 | 跨模块多文件             |
| 难度     | 困难                     |
| 技术栈   | Rust, clap, serde_json   |
| 添加时间 | 2026-05-12 09:00         |
| 已交付   | 否                       |

**题目：**
在现有审批路由引擎基础上增加条件表达式解析器、多分支条件匹配和会签投票机制，支持 AND/OR/比较运算符和通过比例配置

**Prompt：**
这个项目是一个审批路由引擎，目前只支持线性的审批链路。我要你给它加上条件路由引擎，具体要求：第一，支持条件表达式解析，表达式语法是 amount > 10000 AND department == "finance" 这种，需要写一个表达式解析器，支持大于、小于、等于、包含、AND、OR 这些操作符，支持字符串和数字类型。第二，条件分支节点可以有多条出口，每条出口绑定一个条件表达式，路由时按优先级依次匹配，匹配到就走那条路。第三，加一个会签节点类型，会签节点需要所有审批人都通过才算通过，任一人拒绝就拒绝，要支持配置通过比例比如"超过60%通过即可"。第四，写集成测试覆盖条件解析、多分支路由、会签投票这些场景。现有的线性路由功能不能破坏。

---

## q30 - 审批超时升级与前端通知联动

| 字段     | 值                                                 |
| -------- | -------------------------------------------------- |
| 名称     | 审批超时升级与前端通知联动                         |
| 分支名字 | escalation-notify                                  |
| 任务类型 | Feature 迭代                                       |
| 业务领域 | 全栈 Web 应用                                      |
| 修改范围 | 跨系统多模块                                       |
| 难度     | 困难                                               |
| 技术栈   | Rust, axum, tokio, chrono, Vue 3, Vite, TypeScript |
| 添加时间 | 2026-05-12 09:00                                   |
| 已交付   | 否                                                 |

**题目：**
全栈项目：后端 axum HTTP API + 定时超时检查升级，前端 Vue3 审批监控页面，10秒轮询刷新，超时标红，升级事件确认

**Prompt：**
这个项目是一个审批超时升级引擎，后端是 Rust 写的，前端是 Vue3。现在后端只能检测超时和打印日志，前端是个空壳。我要你做这些：后端加一个 HTTP API 服务，用 axum 实现，提供三个接口：GET /api/pending 返回所有待审批请求列表，GET /api/escalations 返回超时升级事件列表，POST /api/escalations/{id}/ack 确认升级事件。后端还要加一个定时检查器，每隔配置的时间间隔扫描一次请求，发现超时的自动升级并写入事件记录，升级规则是：超时后自动跳到上一级审批人，最多升3级。前端要做一个审批监控页面，顶部显示待审批数量和超时预警数量的统计卡片，中间是待审批列表，每行显示请求ID、申请人、当前审批人、已等待时间，超时的行标红。页面要每10秒自动刷新数据。升级事件确认后要从预警列表移除。

---

## q31 - 配置热重载与审批指标联合增强

| 字段     | 值                                           |
| -------- | -------------------------------------------- |
| 名称     | 配置热重载与审批指标联合增强                 |
| 分支名字 | cfg-metrics-fusion                           |
| 任务类型 | Feature 迭代                                 |
| 业务领域 | 命令行工具                                   |
| 修改范围 | 跨模块多文件                                 |
| 难度     | 困难                                         |
| 技术栈   | Rust, clap, configparser, serde_json, chrono |
| 添加时间 | 2026-05-12 09:00                             |
| 已交付   | 否                                           |

**题目：**
整合 cfg-hotreload 和 approval-metrics 两个 CLI 工具，在配置文件增加 [metrics] 段，daemon 模式下文件变更自动重算指标，支持 --metrics-only 参数

**Prompt：**
我有两个 Rust CLI 工具，一个是 cfg-hotreload 做配置热重载和数据导出，一个是 approval-metrics 做审批指标统计。我要你把它们整合到 cfg-hotreload 项目里，具体要求：第一，在 cfg-hotreload 的配置文件里新增一个 [metrics] 段，可以配置审批记录的 JSON 文件路径和统计维度。第二，cfg-hotreload 启动时如果配置了 metrics 段，就加载审批记录并计算指标，指标包括审批通过率、平均审批时长、瓶颈审批人，计算逻辑参考 approval-metrics 的实现。第三，在 daemon 模式下，如果审批记录文件发生变化（通过文件修改时间检测），自动重新计算指标并输出到终端。第四，加一个 --metrics-only 参数，只跑指标计算不跑数据导出。第五，指标输出支持 terminal 和 json 两种格式，通过配置文件控制。现有的数据导出和热重载功能不能受影响。

---

## q32 - 标准文档批量解析状态追踪

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 名称     | 标准文档批量解析状态追踪              |
| 分支名字 | tsp2-batch-parse-tracker              |
| 任务类型 | Feature 迭代                          |
| 业务领域 | 全栈 Web 应用                         |
| 修改范围 | 跨系统多模块                          |
| 难度     | 困难                                  |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                      |
| 已交付   | 否                                    |

**题目：**
在现有标准文档上传解析功能基础上，增加批量上传的解析状态追踪能力，支持ZIP包内逐文件进度展示、失败重试、解析耗时统计

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前标准文档上传支持单文件和ZIP批量上传，但ZIP上传后用户看不到每个文件的解析进度，只能等全部完成。我要你做这些：第一，后端在 StandardDoc 实体上增加 parseStatus（解析中/成功/失败）、parseProgress（0-100百分比）、parseStartTime、parseEndTime、parseError 字段。第二，后端新增一个批量解析任务表 BatchParseTask，记录每次ZIP上传的任务ID、总文件数、已成功数、已失败数、整体状态。第三，后端新增 GET /api/standardDoc/batch/{taskId} 接口返回批量任务进度，GET /api/standardDoc/batch/{taskId}/details 返回每个文件的解析状态列表。第四，ZIP上传后改为异步逐文件解析，每个文件解析完更新进度，失败的记录错误信息但不阻塞后续文件。第五，前端在标准管理-上传页面增加一个"批量上传进度"对话框，上传ZIP后自动弹出，展示每个文件名、解析状态（带进度条）、耗时，失败的显示错误原因和"重试"按钮。第六，前端每3秒轮询批量任务进度接口更新状态。现有的单文件上传和ZIP上传功能不能受影响。

---

## q33 - 标准文档版本对比与变更追踪

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 名称     | 标准文档版本对比与变更追踪            |
| 分支名字 | tsp2-version-diff                     |
| 任务类型 | Feature 迭代                          |
| 业务领域 | 全栈 Web 应用                         |
| 修改范围 | 跨系统多模块                          |
| 难度     | 困难                                  |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                      |
| 已交付   | 否                                    |

**题目：**
在标准文档管理中增加版本管理能力，支持同一标准编号多版本存储、版本间关键字段差异对比、变更历史时间线展示

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前同一个标准编号只能存一条记录，如果标准更新了只能覆盖。我要你做这些：第一，后端在 StandardDoc 实体增加 version（版本号，如2023、2024）和 parentId（指向上一版本的ID）字段，同一个 standardNo 可以存在多条不同版本的记录。第二，上传时如果检测到已有相同 standardNo 的记录，自动将新记录的 version 递增，并设置 parentId 指向上一版本。第三，后端新增 GET /api/standardDoc/{id}/versions 接口返回该标准所有版本列表（按版本号倒序），GET /api/standardDoc/diff?id1=xxx&id2=xxx 接口对比两个版本的关键字段差异，返回格式为 [{field: "standardName", old: "旧名称", new: "新名称"}, ...]，对比字段包括 standardName、standardLevelName、publishDate、implementDate、publishUnit、draftUnit、scope、preface、normativeReferences。第四，前端在标准详情页增加"版本历史"Tab页签，展示版本时间线（竖向时间轴），每个节点显示版本号和发布日期，点击两个版本可以查看差异对比。第五，差异对比用左右对照表格展示，变更的字段高亮标黄，新增内容标绿，删除内容标红。现有的标准查询和详情功能不能受影响。

---

## q34 - 知识图谱可视化交互增强

| 字段     | 值                                 |
| -------- | ---------------------------------- |
| 名称     | 知识图谱可视化交互增强             |
| 分支名字 | tsp2-graph-visual-enhance          |
| 任务类型 | Feature 迭代                       |
| 业务领域 | 全栈 Web 应用                      |
| 修改范围 | 跨系统多模块                       |
| 难度     | 困难                               |
| 技术栈   | Spring Boot, Neo4j, Vue 3, ECharts |
| 添加时间 | 2026-05-13 10:00                   |
| 已交付   | 否                                 |

**题目：**
增强现有知识图谱功能，支持多层级展开收缩、节点搜索定位、关系路径查询、图谱截图导出

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + Neo4j，前端 Vue 3 + ECharts。目前知识图谱页面只能展示一个标准的节点和关系，没有交互能力。我要你做这些：第一，后端新增 GET /api/knowledge-graph/expand?nodeId=xxx&depth=2 接口，支持从指定节点向外展开N层关系，返回新增的节点和边。第二，后端新增 GET /api/knowledge-graph/search?keyword=xxx 接口，在图谱中搜索节点名称，返回匹配的节点ID列表。第三，后端新增 GET /api/knowledge-graph/path?from=xxx&to=xxx 接口，查询两个标准之间的最短引用路径，返回路径上的节点和关系列表。第四，前端图谱改为力导向图（ECharts graph），默认只展示当前标准的1层关系，双击节点可以展开下一层关系（调用expand接口），右键节点可以收缩已展开的子节点。第五，前端增加搜索框，输入关键词后高亮匹配的节点并自动居中定位。第六，前端增加"路径查询"功能，选择两个标准后展示它们之间的引用路径，路径上的节点和边用不同颜色高亮。第七，前端增加"导出图片"按钮，将当前图谱截图保存为PNG。现有的图谱展示功能不能受影响。

---

## q35 - 标准文档智能纠错与建议

| 字段     | 值                                                |
| -------- | ------------------------------------------------- |
| 名称     | 标准文档智能纠错与建议                            |
| 分支名字 | tsp2-smart-proofread                              |
| 任务类型 | Feature 迭代                                      |
| 业务领域 | 全栈 Web 应用                                     |
| 修改范围 | 跨系统多模块                                      |
| 难度     | 困难                                              |
| 技术栈   | Spring Boot, JPA, AI Service, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                                  |
| 已交付   | 否                                                |

**题目：**
在标准文档详情页增加AI智能纠错功能，自动检测标准编号格式、日期逻辑、规范性引用一致性等问题，支持一键修正

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus，已有AI服务调用能力。目前标准文档入库后没有质量校验，经常出现编号格式不规范、实施日期早于发布日期、引用的标准不存在等问题。我要你做这些：第一，后端新增校验服务 StandardDocValidationService，实现以下校验规则：标准编号格式校验（GB/T xxxxx-xxxx、YC/T xxx-xxxx等标准格式正则匹配）、日期逻辑校验（实施日期不应早于发布日期）、规范性引用一致性校验（引用的标准编号在数据库中是否存在）、必填字段完整性校验（standardNo、standardName、publishDate不能为空）。第二，后端新增 POST /api/standardDoc/{id}/validate 接口，对指定标准执行校验，返回校验结果列表 [{rule: "编号格式", status: "pass/error", message: "具体描述", suggestion: "修正建议"}]。第三，后端新增 POST /api/standardDoc/{id}/auto-fix 接口，对可自动修正的问题执行修正（如编号格式标准化、日期互换），返回修正前后的对比。第四，前端在标准详情页基础信息Tab下方增加"智能纠错"区域，点击"开始校验"按钮调用validate接口，展示校验结果列表，每条结果用不同颜色图标标识（通过/警告/错误）。第五，对于有suggestion的校验项，显示"一键修正"按钮，点击调用auto-fix接口并刷新页面。第六，校验结果要缓存，不要每次打开详情页都重新校验。现有的详情页展示功能不能受影响。

---

## q36 - 标准体系树形管理与统计

| 字段     | 值                                             |
| -------- | ---------------------------------------------- |
| 名称     | 标准体系树形管理与统计                         |
| 分支名字 | tsp2-standard-system-tree                      |
| 任务类型 | Feature 迭代                                   |
| 业务领域 | 全栈 Web 应用                                  |
| 修改范围 | 跨系统多模块                                   |
| 难度     | 困难                                           |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus, ECharts |
| 添加时间 | 2026-05-13 10:00                               |
| 已交付   | 否                                             |

**题目：**
增强标准体系管理功能，支持树形层级结构维护、拖拽排序、体系下标准数量统计、体系健康度评分

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus + ECharts。目前标准体系（StandardSystem）是扁平列表，没有层级关系，也没有统计功能。我要你做这些：第一，后端在 StandardSystem 实体增加 parentId 字段，支持树形层级结构，同一 parentId 下用 sortOrder 字段排序。第二，后端新增 GET /api/standardSystem/tree 接口返回树形结构，POST /api/standardSystem/move 接口支持移动节点（改变parentId和sortOrder），GET /api/standardSystem/{id}/stats 接口返回该体系下标准数量、按标准级别分布、按发布年份分布。第三，后端新增 GET /api/standardSystem/{id}/health 接口计算体系健康度评分，评分维度包括：标准覆盖率（有标准的子体系占比）、标准时效性（5年内更新的标准占比）、引用完整性（规范性引用都能找到的标准占比），返回各维度分数和总分（0-100）。第四，前端新增标准体系管理页面，左侧是可展开收缩的树形结构（Element Plus Tree），支持拖拽节点调整层级和排序，右键菜单支持新增/编辑/删除子体系。第五，右侧展示选中体系的统计信息：标准总数、级别分布饼图、年份分布柱状图、健康度雷达图。第六，体系名称支持搜索过滤。现有的标准体系CRUD功能不能受影响。

---

## q37 - 标准文档目录结构化编辑器

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 名称     | 标准文档目录结构化编辑器              |
| 分支名字 | tsp2-toc-editor                       |
| 任务类型 | Feature 迭代                          |
| 业务领域 | 全栈 Web 应用                         |
| 修改范围 | 跨系统多模块                          |
| 难度     | 困难                                  |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                      |
| 已交付   | 否                                    |

**题目：**
增强标准文档目录（TOC）功能，支持树形可视化编辑、拖拽调整章节顺序、批量修改章节层级、目录模板复用

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前标准文档目录（StandardDocToc）是AI解析后自动生成的，用户无法手动编辑和调整。我要你做这些：第一，后端在 StandardDocToc 实体增加 sortOrder 字段，支持同级目录排序。第二，后端新增 PUT /api/standardDocToc/batch-update 接口，支持批量更新目录项的层级（level）和排序（sortOrder），请求体为 [{id: 1, level: 2, sortOrder: 1}, ...]。第三，后端新增 POST /api/standardDocToc/template 接口，将当前标准的目录结构保存为模板（存到新表 TocTemplate），GET /api/standardDocToc/template/list 返回模板列表，POST /api/standardDocToc/apply-template 接口将模板应用到指定标准（清空原有目录，按模板创建新目录项）。第四，前端在标准详情页的"章节目录"Tab页签增加编辑模式，点击"编辑目录"按钮进入编辑模式，目录以树形结构展示，支持拖拽调整顺序和层级（拖拽时实时更新缩进和编号）。第五，编辑模式下支持新增/删除/重命名目录项，新增时自动生成编号（如3.1、3.2）。第六，增加"保存为模板"和"应用模板"按钮，应用模板时弹出模板选择对话框。现有的目录展示功能不能受影响，编辑模式需要手动进入。

---

## q38 - 标准文档收藏与对比阅读

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 名称     | 标准文档收藏与对比阅读                |
| 分支名字 | tsp2-favorite-compare                 |
| 任务类型 | Feature 迭代                          |
| 业务领域 | 全栈 Web 应用                         |
| 修改范围 | 跨系统多模块                          |
| 难度     | 困难                                  |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                      |
| 已交付   | 否                                    |

**题目：**
在标准智能查询中增加收藏夹和双文档对比阅读功能，支持收藏分组、标签管理、双栏同步滚动对比

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前标准查询列表有收藏按钮但只是简单标记，没有收藏管理功能，也没有文档对比能力。我要你做这些：第一，后端新增收藏夹表 StandardDocFavorite，字段包括 id、userId、standardDocId、groupId、tags（逗号分隔）、createdAt。第二，后端新增收藏分组表 FavoriteGroup，字段包括 id、userId、groupName、sortOrder。第三，后端新增接口：POST /api/favorite/toggle 收藏/取消收藏，GET /api/favorite/list?groupId=xxx 分组查询收藏列表，POST /api/favorite/group 新增分组，PUT /api/favorite/move 移动收藏到其他分组，PUT /api/favorite/tags 更新收藏标签。第四，前端在AI管理-智能查询页面左侧增加"我的收藏"侧边栏，展示收藏分组树，点击分组显示该分组下的标准列表，支持拖拽标准到不同分组。第五，前端新增"对比阅读"功能，在标准列表勾选两个标准后点击"对比"按钮，打开双栏对比页面，左右各展示一个标准的基础信息和章节目录，滚动时左右同步联动。第六，对比页面中两个标准的相同章节自动对齐高亮，不同章节用不同底色标识。现有的收藏标记和查询功能不能受影响。

---

## q39 - 标准文档Markdown在线预览与编辑

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 名称     | 标准文档Markdown在线预览与编辑        |
| 分支名字 | tsp2-md-preview-edit                  |
| 任务类型 | Feature 迭代                          |
| 业务领域 | 全栈 Web 应用                         |
| 修改范围 | 跨系统多模块                          |
| 难度     | 困难                                  |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                      |
| 已交付   | 否                                    |

**题目：**
在标准详情页增加Markdown在线预览和编辑功能，支持实时预览、目录导航跳转、编辑历史回溯、Markdown导出

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前AI解析标准PDF后会生成Markdown文件存在服务器磁盘上，但前端只能看基础信息，无法查看和编辑Markdown内容。我要你做这些：第一，后端新增 GET /api/standardDoc/{id}/markdown 接口，读取服务器上的Markdown文件内容返回给前端，如果文件不存在返回空字符串。第二，后端新增 PUT /api/standardDoc/{id}/markdown 接口，接收前端提交的Markdown内容，保存到磁盘并更新 mdFilePath 字段。第三，后端新增 MarkdownEditHistory 表，记录每次编辑的 id、standardDocId、content（编辑后内容）、editedAt、editor，提供 GET /api/standardDoc/{id}/markdown/history 接口返回编辑历史列表，POST /api/standardDoc/{id}/markdown/rollback?historyId=xxx 接口回滚到指定历史版本。第四，前端在标准详情页增加"Markdown"Tab页签，左侧是Markdown编辑器（使用CodeMirror或mavonEditor），右侧是实时预览区域，编辑时预览同步更新。第五，预览区域顶部自动提取Markdown标题生成目录导航，点击目录项跳转到对应章节。第六，增加"保存"按钮保存编辑内容，"历史"按钮查看编辑历史并支持回滚，"导出"按钮将Markdown下载为.md文件。现有的详情页其他Tab功能不能受影响。

---

## q40 - 标准合规性审查报告生成

| 字段     | 值                                             |
| -------- | ---------------------------------------------- |
| 名称     | 标准合规性审查报告生成                         |
| 分支名字 | tsp2-compliance-report                         |
| 任务类型 | Feature 迭代                                   |
| 业务领域 | 全栈 Web 应用                                  |
| 修改范围 | 跨系统多模块                                   |
| 难度     | 困难                                           |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus, ECharts |
| 添加时间 | 2026-05-13 10:00                               |
| 已交付   | 否                                             |

**题目：**
新增标准合规性审查功能，支持选择标准集合进行合规性检查、生成审查报告、报告导出PDF、审查结果可视化

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus + ECharts。目前系统只有标准查询和知识图谱功能，没有合规性审查能力。我要你做这些：第一，后端新增 ComplianceReport 表，字段包括 id、reportName、standardIds（逗号分隔）、status（生成中/已完成/失败）、createdAt、completedAt、result（JSON存储审查结果）。第二，后端新增合规性审查服务 ComplianceCheckService，审查维度包括：标准时效性（是否过期或即将过期）、引用完整性（引用的标准是否都存在）、格式规范性（编号格式是否符合国标/行标规范）、体系覆盖度（该标准所属体系下标准的覆盖情况）。第三，后端新增 POST /api/compliance/check 接口启动审查任务（异步执行），GET /api/compliance/report/{id} 返回报告详情，GET /api/compliance/reports 返回报告列表，GET /api/compliance/report/{id}/export 导出报告为PDF。第四，前端新增合规性审查页面，包含"新建审查"按钮（弹出对话框选择要审查的标准集合）和审查报告列表。第五，报告详情页展示审查概要（总标准数、通过数、警告数、错误数），各维度检查结果的详细列表，以及合规性评分雷达图。第六，报告支持导出PDF（前端使用html2canvas + jsPDF实现）。现有的标准查询和管理功能不能受影响。

---

## q41 - 标准文档变更通知与订阅

| 字段     | 值                                                    |
| -------- | ----------------------------------------------------- |
| 名称     | 标准文档变更通知与订阅                                |
| 分支名字 | tsp2-change-notify                                    |
| 任务类型 | Feature 迭代                                          |
| 业务领域 | 全栈 Web 应用                                         |
| 修改范围 | 跨系统多模块                                          |
| 难度     | 困难                                                  |
| 技术栈   | Spring Boot, JPA, Scheduled Task, Vue 3, Element Plus |
| 添加时间 | 2026-05-13 10:00                                      |
| 已交付   | 否                                                    |

**题目：**
新增标准变更订阅与通知功能，支持按标准体系/级别/关键词订阅、变更事件检测、站内消息通知、通知偏好设置

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus。目前标准文档更新后没有任何通知机制，用户无法及时了解关注的标准是否有变化。我要你做这些：第一，后端新增 StandardSubscription 表，字段包括 id、userId、subscriptionType（system/level/keyword）、subscriptionValue（体系ID/级别名称/关键词）、createdAt。第二，后端新增 StandardChangeEvent 表，字段包括 id、standardDocId、changeType（新增/更新/废止）、changeDetail（JSON，记录变更字段和前后值）、detectedAt。第三，后端新增定时任务 ChangeDetectionTask，每小时执行一次，对比最近一小时内有变动的标准（通过 updatedAt 字段判断），生成变更事件，匹配订阅规则，写入 Notification 表（字段：id、userId、eventId、isRead、createdAt）。第四，后端新增接口：POST /api/subscription 订阅，DELETE /api/subscription/{id} 取消订阅，GET /api/subscription/list 我的订阅列表，GET /api/notification/list?isRead=xxx 通知列表，PUT /api/notification/{id}/read 标记已读，PUT /api/notification/read-all 全部已读，GET /api/notification/unread-count 未读数量。第五，前端在页面顶部导航栏增加通知铃铛图标，显示未读数量角标，点击展开通知列表（最近20条），每条通知显示标准名称、变更类型、变更时间，点击跳转到标准详情页。第六，前端新增"我的订阅"页面，展示当前订阅列表，支持新增订阅（选择类型和值），删除订阅。现有的标准管理功能不能受影响。

---

## q42 - 标准统计分析多维报表

| 字段     | 值                                             |
| -------- | ---------------------------------------------- |
| 名称     | 标准统计分析多维报表                           |
| 分支名字 | tsp2-stats-report                              |
| 任务类型 | Feature 迭代                                   |
| 业务领域 | 全栈 Web 应用                                  |
| 修改范围 | 跨系统多模块                                   |
| 难度     | 困难                                           |
| 技术栈   | Spring Boot, JPA, Vue 3, Element Plus, ECharts |
| 添加时间 | 2026-05-13 10:00                               |
| 已交付   | 否                                             |

**题目：**
增强标准统计分析功能，支持多维度交叉分析、自定义报表配置、报表数据钻取、报表导出Excel

**Prompt：**
这是一个烟草标准管理系统，后端 Spring Boot + JPA，前端 Vue 3 + Element Plus + ECharts。目前统计分析页面只有简单的图表展示，缺少灵活的多维分析能力。我要你做这些：第一，后端新增统计分析服务 StatisticsAnalysisService，支持以下维度交叉组合查询：按标准级别（GB/YC/HNYQ等）、按标准体系、按发布年份、按起草单位、按标准状态（现行/废止）。第二，后端新增 POST /api/statistics/analyze 接口，接收分析参数 {dimensions: ["level", "year"], filters: {systemId: "xxx", yearRange: [2020, 2025]}, metrics: ["count", "avgAge"]}，返回交叉分析结果。第三，后端新增 GET /api/statistics/export 接口，将分析结果导出为Excel文件（使用Apache POI）。第四，前端重构统计分析页面，左侧是维度和筛选条件配置面板，支持拖拽维度到行/列区域，支持添加筛选条件（标准体系多选、年份范围滑块、标准级别多选）。第五，右侧根据配置动态生成图表，支持切换图表类型（柱状图/折线图/饼图/热力图），点击图表数据点可以下钻到下一层级（如点击某年份柱子展开该年份的季度数据）。第六，增加"导出Excel"按钮，调用后端导出接口下载当前分析结果。现有的统计分析页面功能不能受影响。
